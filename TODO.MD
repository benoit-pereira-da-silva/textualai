1. ollama, should be only a thin adapter layer on the top of the openai api 
2. find a way to elegantly display the thinking layer.
3. when a model can't use tools we should propose to switch. 
3. We should have a client that adds dynamically tools for example to pull models.
